import React, { Component } from 'react';

/**
 * Defines the Methods component. Renders the component when the methods url is reached.
 */
export default class Methods extends Component {
    render() {
        return (
            <main className="methods">
                <h1>Methods</h1>
            </main>
        );
    }
}