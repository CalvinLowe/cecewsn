import React, { Component } from 'react';

/**
 * Defines the Help component. Renders the Help page when the URL is reached.
 */
export default class Help extends Component {
    render() {
        return (
            <main className="help">
                <h1>Help</h1>
            </main>
        );
    }
}